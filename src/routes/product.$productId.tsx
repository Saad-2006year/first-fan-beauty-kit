import { createFileRoute, Link, notFound } from "@tanstack/react-router";
import { Check, Minus, Plus, ShoppingBag } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { money, products, useCart } from "@/lib/store";

export const Route = createFileRoute("/product/$productId")({ loader: ({ params }) => { const product = products.find((item) => item.id === params.productId); if (!product) throw notFound(); return product; }, head: ({ loaderData }) => ({ meta: [{ title: loaderData ? `${loaderData.name} — FIRST FAN` : "Product unavailable — FIRST FAN" }, { name: "description", content: loaderData?.description ?? "The requested product is unavailable." }, { property: "og:title", content: loaderData ? `${loaderData.name} — FIRST FAN` : "Product unavailable" }, { property: "og:description", content: loaderData?.description ?? "The requested product is unavailable." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: ProductPage });
function ProductPage() { const product = Route.useLoaderData(); const [quantity, setQuantity] = useState(1); const { add } = useCart(); return <main className="mx-auto grid max-w-6xl gap-8 px-4 py-8 md:grid-cols-2 lg:px-8"><img src={product.image} alt={product.name} width={816} height={816} className="aspect-square w-full rounded-[2rem] object-cover" /><div className="md:py-8"><p className="text-xs font-extrabold uppercase text-accent">{product.category}</p><h1 className="mt-2 font-display text-4xl font-bold md:text-5xl">{product.name}</h1><p className="mt-2 text-sm text-muted-foreground">{product.detail}</p><p className="mt-5 text-lg leading-relaxed">{product.description}</p><p className="mt-6 font-display text-3xl font-bold">{money(product.price)}</p><div className="mt-6 flex gap-3"><div className="flex h-12 items-center rounded-xl border-2 border-border bg-card"><Button variant="ghost" size="icon" onClick={() => setQuantity((q) => Math.max(1, q - 1))}><Minus /></Button><span className="w-8 text-center font-bold">{quantity}</span><Button variant="ghost" size="icon" onClick={() => setQuantity((q) => q + 1)}><Plus /></Button></div><Button className="h-12 flex-1 rounded-xl" onClick={() => { for (let i = 0; i < quantity; i += 1) add(product.id); }}><ShoppingBag /> Add to cart</Button></div><div className="mt-8 rounded-3xl bg-secondary p-5"><h2 className="font-display text-xl font-bold">What’s inside</h2><div className="mt-3 grid gap-2">{product.ingredients.map((item) => <div key={item} className="flex items-center gap-2 text-sm"><Check className="size-4 text-primary" />{item}</div>)}</div></div><Button asChild variant="link" className="mt-4 px-0"><Link to="/shop">← Back to shop</Link></Button></div></main>; }