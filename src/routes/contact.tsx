import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { PageIntro } from "@/components/product-grid";
export const Route = createFileRoute("/contact")({ head: () => ({ meta: [{ title: "Contact — FIRST FAN" }, { name: "description", content: "Get product guidance and order help from FIRST FAN." }, { property: "og:title", content: "Contact — FIRST FAN" }, { property: "og:description", content: "Questions about your glow routine? We are here to help." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Contact });
function Contact() { const [sent, setSent] = useState(false); return <main className="mx-auto max-w-3xl px-4 py-12 lg:px-8"><PageIntro eyebrow="Say hello" title="Let’s talk glow" text="Ask about products, routines or your order. Our team will get back to you." />{sent ? <div className="rounded-3xl bg-secondary p-8 text-center"><h2 className="font-display text-2xl font-bold">Message received!</h2><p className="mt-2 text-muted-foreground">We’ll be in touch soon.</p></div> : <form className="grid gap-4 rounded-3xl border-2 border-border bg-card p-5" onSubmit={(event) => { event.preventDefault(); setSent(true); }}><Input required placeholder="Your name" className="h-12 rounded-xl" /><Input required type="email" placeholder="Email address" className="h-12 rounded-xl" /><textarea required placeholder="How can we help?" className="min-h-36 rounded-xl border border-input bg-background p-3 outline-none focus:ring-2 focus:ring-ring" /><Button type="submit" className="h-12 rounded-xl">Send message</Button></form>}</main>; }