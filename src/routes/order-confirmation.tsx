import { createFileRoute, Link } from "@tanstack/react-router";
import { CheckCircle2 } from "lucide-react";
import { Button } from "@/components/ui/button";
export const Route = createFileRoute("/order-confirmation")({ validateSearch: (search: Record<string, unknown>) => ({ code: typeof search["code"] === "string" ? search["code"] : "" }), head: () => ({ meta: [{ title: "Order Confirmed — FIRST FAN" }, { name: "description", content: "Your FIRST FAN order is confirmed." }, { property: "og:title", content: "Order Confirmed — FIRST FAN" }, { property: "og:description", content: "Your glow is on the way." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Confirmation });
function Confirmation() { const { code } = Route.useSearch(); return <main className="mx-auto max-w-xl px-4 py-20 text-center"><CheckCircle2 className="mx-auto size-16 text-primary" /><p className="mt-5 text-xs font-extrabold uppercase text-accent">Order confirmed</p><h1 className="mt-2 font-display text-4xl font-bold">Your glow is on the way!</h1><p className="mt-3 text-muted-foreground">Tracking code: <strong className="text-foreground">{code || "Available in your account"}</strong></p><div className="mt-7 flex justify-center gap-3"><Button asChild className="rounded-xl"><Link to="/tracking">Track order</Link></Button><Button asChild variant="outline" className="rounded-xl"><Link to="/shop">Keep shopping</Link></Button></div></main>; }